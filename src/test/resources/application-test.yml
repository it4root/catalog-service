spring:
  datasource:
    ##url: jdbc:postgresql://localhost:5432/catalog_test_db
    username: test_db
    password: test_db
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
docker:
  postgress: postgres:12.1